@model BibliotecaMetropolis.Models.Autor

@{
    ViewData["Title"] = "Editar Autor";
    
}

<div class="container my-5 text-glow-light">
    <h1 class="fw-bold text-glow-primary">
        <i class="bi bi-pencil-square me-2"></i> Editar Autor
    </h1>

    <h4 class="text-glow-success">
        Catálogo de Creadores
    </h4>
    <hr class="glow-hr mb-4" />

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card card-futuristic p-4 card-zoom-sm">

                <form asp-action="Edit">

                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <input type="hidden" asp-for="IdAutor" />

                    <h5 class="fw-bold text-glow-warning mb-3">Datos del Autor</h5>

                    <div class="form-group mb-3">
                        <label asp-for="Nombres" class="control-label text-white fw-bold"></label>
                        <input asp-for="Nombres" class="form-control form-control-futuristic" />
                        <span asp-validation-for="Nombres" class="text-glow-danger small"></span>
                    </div>

                    <div class="form-group mb-4">
                        <label asp-for="Apellidos" class="control-label text-white fw-bold"></label>
                        <input asp-for="Apellidos" class="form-control form-control-futuristic" />
                        <span asp-validation-for="Apellidos" class="text-glow-danger small"></span>
                    </div>

                    <div class="form-group d-grid gap-2 mt-4">
                        <input type="submit" value="Guardar Cambios" class="btn btn-primary btn-futuristic" />
                    </div>
                </form>

            </div>
        </div>
    </div>

    <div class="mt-4 text-center">
        <a asp-action="Index" class="text-decoration-none text-glow-warning">
            <i class="bi bi-arrow-left-circle me-1"></i> Volver a la Lista de Autores
        </a>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}